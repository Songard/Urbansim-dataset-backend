# ==================================================
# 下载性能优化配置文件
# ==================================================
# 将此文件复制为 .env 或合并到现有 .env 文件中

# 下载优化设置
# 使用更大的chunk size可以显著提升大文件下载速度
# 推荐值：32MB适用于大多数文件，64MB适用于超大文件(>1GB)
DOWNLOAD_CHUNK_SIZE_MB=64

# 增加超时时间以处理网络波动和大chunk传输
# 大chunk需要更长时间传输，避免超时中断
DOWNLOAD_TIMEOUT=600

# 更多重试次数确保下载可靠性
# 网络不稳定时多次重试可以提高成功率
DOWNLOAD_RETRIES=5

# 并发下载数量
# 注意：过多并发可能导致API限制
MAX_CONCURRENT_DOWNLOADS=2

# HTTP连接优化
ENABLE_HTTP2=True

# 其他相关性能设置
CHECK_INTERVAL=15
RETRY_DELAY=3
BATCH_WRITE_SIZE=20

# ==================================================
# 性能对比分析
# ==================================================
#
# 1MB chunks  : 慢 - 1GB文件需要1024次请求
# 8MB chunks  : 一般 - 1GB文件需要128次请求  
# 32MB chunks : 好 - 1GB文件需要32次请求 ✅
# 64MB chunks : 最佳 - 1GB文件需要16次请求 🚀
#
# 建议根据你的网络状况和文件大小选择：
# - 小文件 (<100MB): 8-16MB chunks
# - 中等文件 (100MB-1GB): 32MB chunks
# - 大文件 (>1GB): 64MB chunks
#
# ==================================================